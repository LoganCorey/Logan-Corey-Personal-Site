import React from "react"
import classes from './Button.module.css';
const AboutButton = () => {
  return <a href="#Projects" className={classes.button}>View Projects</a>
}

export default AboutButton
